namespace GeoGen.Core
{
    /// <summary>
    /// Represents a way that loose objects of a configuration are arranged.  
    /// </summary>
    public enum LooseObjectsLayout
    {
        /// <summary>
        /// Represented a triangle which angles are all acute and their mutual differences
        /// are at least some constant value (so the triangle is not isosceles).
        /// </summary>
        ScaleneAcuteAngledTriangled
    }
}